#module = a file containing python code. May contain functions, classes, etc.

from messages import hello, bye

hello()
bye()

# Usando * llamamos a todas las funciones, pero no es recomendable ya que pueden molestarse unas a otras

help("modules")
#Con esta función podemos ver las diferentes funciones de las que disponemos

